import { Box, styled } from "@mui/material";

namespace S {
  export const FarmersGroupContainer = styled(Box)(({ theme }) => ({
    width: "100%",
    height: "100%",
  }));
}

export default S;
