import styled from "@emotion/styled";
import { Box, Grid } from "@mui/material";

namespace S {
  export const MdDetailsTableContainer = styled(Box)(({ theme }: any) => ({
    width: "100%",
    height: "100%",
    margin: "0 auto",
  }));
}

export default S;
