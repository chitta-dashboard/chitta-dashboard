import { Autocomplete, styled } from "@mui/material";

namespace S {
  export const StyledAutocomplete = styled(Autocomplete)({
    width: "18.75rem",
    height: "2.5rem",
  });
}

export default S;
